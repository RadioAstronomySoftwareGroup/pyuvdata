# conda environment specification for Read the Docs
name: rtd
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.6
  - astropy
  - numpy
  - pip:
    - h5py
    - pypandoc
    - scipy
    - setuptools_scm
